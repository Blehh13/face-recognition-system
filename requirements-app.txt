# Runtime dependencies of the web application only, used by the Dockerfile.
#
# Derived from app.py's actual import closure: PIL, cv2, flask, numpy. The
# default engine is YuNet + SFace via opencv-python, so dlib is not here and
# the image needs no compiler.

numpy>=1.26
Pillow>=10.4

# Headless build: identical API, without the X11/GL libraries the desktop
# wheel pulls in. Provides FaceDetectorYN and FaceRecognizerSF.
opencv-python-headless>=4.5.4

Flask>=3.0
gunicorn>=21.2
