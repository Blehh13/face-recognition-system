# Runtime dependencies of the web application only.
#
# Derived from app.py's actual import closure: PIL, cv2, face_recognition,
# flask, numpy. scikit-learn, matplotlib, seaborn, tqdm, click and pytest are
# used by the CLI, the evaluator and the test suite — none of which the server
# imports — and together they accounted for most of an 841 MB site-packages
# layer in the container image.
#
# Use requirements.txt for development; this file is for the Dockerfile.

face-recognition>=1.2.3
face-recognition-models>=0.1.0
numpy>=1.26
Pillow>=10.4

# Headless build: identical API, without the X11/GL libraries that the
# desktop wheel pulls in. Only cli.py's --show needs a display, and that
# does not run in a container.
opencv-python-headless>=4.5.4

# face_recognition_models imports pkg_resources, removed in setuptools 81.
setuptools<81

Flask>=3.0
gunicorn>=21.2
