# Verified on Python 3.14.6 / Windows 11. The full test suite (100 tests)
# passes against exactly these versions.
#
# Lower bounds rather than hard pins: the previous file pinned versions that
# were never actually installed here (numpy 1.26 against 2.5 in use, opencv
# 4.10 against 5.0), so `pip install -r requirements.txt` would have produced
# an environment nobody had tested.

# --- Core recognition -------------------------------------------------------
face-recognition>=1.2.3
face-recognition-models>=0.1.0
numpy>=1.26
Pillow>=10.4

# opencv-python 4.5.4+ is required for cv2.FaceDetectorYN / cv2.FaceRecognizerSF,
# used by the optional engine="opencv" backend (src/opencv_engine.py).
# Verified against 5.0.0.
opencv-python>=4.5.4

# face_recognition_models imports pkg_resources at module load, and setuptools
# removed pkg_resources in 81.0. Without this bound, every dlib-backed call
# fails with "ModuleNotFoundError: No module named 'pkg_resources'".
setuptools<81

# --- Web --------------------------------------------------------------------
Flask>=3.0

# --- CLI --------------------------------------------------------------------
click>=8.1

# --- Evaluation and reporting -----------------------------------------------
scikit-learn>=1.5
matplotlib>=3.9
seaborn>=0.13
tqdm>=4.66

# --- Tests ------------------------------------------------------------------
pytest>=8.3

# --- Training (optional; only needed for the ml/ pipeline) ------------------
# CPU wheels come from PyPI. For GPU, install from the CUDA index instead:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
# Note: PyPI's Windows wheel for Python 3.14 is CPU-only.
torch>=2.4
torchvision>=0.19
