# Verified on Python 3.14.6 / Windows 11. The full test suite passes against
# these versions.
#
# No compiler required. The default engine is YuNet + SFace, which runs through
# opencv-python's own DNN module, so nothing here is built from source.
# dlib is optional — see requirements-dlib.txt.

# --- Core ------------------------------------------------------------------
numpy>=1.26
Pillow>=10.4

# opencv-python 4.5.4+ provides cv2.FaceDetectorYN and cv2.FaceRecognizerSF,
# the default detector and embedder. Verified against 5.0.0. The ONNX weights
# (~37 MB) are downloaded on first use.
opencv-python>=4.5.4

# --- Web -------------------------------------------------------------------
Flask>=3.0

# Only needed to serve behind a real WSGI server (Docker/deployment).
gunicorn>=21.2

# --- CLI -------------------------------------------------------------------
click>=8.1

# --- Evaluation and reporting ----------------------------------------------
# Used by src/evaluator.py and the ml/ benchmarks, not by the web app.
scikit-learn>=1.5
matplotlib>=3.9
seaborn>=0.13
tqdm>=4.66

# --- Tests -----------------------------------------------------------------
pytest>=8.3
